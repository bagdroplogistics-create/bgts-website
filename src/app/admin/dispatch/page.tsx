import { DispatchShell } from '@/components/dispatch/DispatchShell'

export const metadata = { title: 'Dispatch Platform — BGTS' }

export default function DispatchPage() {
  return <DispatchShell />
}
